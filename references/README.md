# References

`references.all.unique.fasta` is the bundled VP1 reference panel used by the wrapper.

Reference headers include analysis group metadata such as `ddns_group` and `cluster`, which the report scripts use to summarize mapped reads and consensus results.

Resource checksums are recorded in `../resources/checksums.sha256`.
