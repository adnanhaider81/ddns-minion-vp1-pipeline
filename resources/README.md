# Resources

`BarcodedPrimers.xlsx` contains the bundled barcode primer definitions used by the wrapper.

The workbook is included because the pipeline requires one forward and one reverse primer sequence per barcode.

Resource checksums are recorded in `checksums.sha256`.
